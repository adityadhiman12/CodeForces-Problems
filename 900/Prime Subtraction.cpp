#include<bits/stdc++.h>
#define endl "\n"
#define ll long long int
using namespace std;


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin>>t;
    while(t--)
	{
		ll a,b;
		cin>>a>>b;
		ll diff=(a-b);
		int flag=0;
		if(diff==1)
			cout<<"NO"<<endl;
		else
			cout<<"YES"<<endl;			
	}
}
